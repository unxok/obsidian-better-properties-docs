import { InfoButton } from "~/components/common/InfoButton";
import Options from "./options.mdx";

#### Arguments

| Name    | Type                                                                 | Default |
| ------- | -------------------------------------------------------------------- | ------- |
| options | <InfoButton label={<code>{"object"}</code>} content={<Options />} /> | -       |

#### Returns

`unknown`

#### Examples

```js
api.getProperty({ property: "myProperty" });

api.getProperty({ property: "myProperty", path: "path/to/note.md" });

api.getProperty({ property: "myProperty", subscribe: false });

api.getProperty({
  property: "myProperty",
  path: "path/to/note.md",
  subscribe: false,
});
```
