#### data

- For file extensions `.csv` and `.tsv` - Will be used as custom delimiter. The default for `.csv` is a comma (`,`). The default for `.tsv` is a tab character (`\t`)
- For all other file extensions - Does nothing.
